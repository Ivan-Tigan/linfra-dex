version: '3.8'

services:
  dex:
    build: .
    container_name: dex-oidc
    ports:
      - "5556:5556"
      - "5558:5558"
    volumes:
      - dex-data:/var/dex
    restart: unless-stopped

volumes:
  dex-data:
